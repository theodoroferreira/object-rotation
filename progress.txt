## Codebase Patterns
- PyOpenGL has no type stubs; mypy.ini configured with `ignore_missing_imports = True` for OpenGL.*
- Camera uses spherical coordinates (distance, azimuth, elevation) converted to cartesian for gluLookAt
- GLUT double-buffered with RGB and depth buffer: `GLUT_DOUBLE | GLUT_RGB | GLUT_DEPTH`
- Use `glPushMatrix/glPopMatrix` + `glTranslatef` to position objects without affecting other draw calls
- `glutWireCube(size)` renders a wireframe cube centered at current matrix position
- Animation uses `glutTimerFunc(16ms, callback, 0)` with self-re-registration for ~60 FPS loop
- `glRotatef(angle, x, y, z)` after `glTranslatef` rotates object in-place around the specified axis

## 2026-04-01 - US-001
- Implemented GLUT window (800x600) with perspective camera positioned diagonally (azimuth=45°, elevation=30°)
- ESC key exits, black background cleared each frame, depth testing enabled
- Files changed: main.py (created full OpenGL app), mypy.ini (created for typecheck config)
- **Learnings for future iterations:**
  - PyOpenGL wildcard imports (`from OpenGL.GL import *`) are standard practice for OpenGL Python code
  - Camera spherical-to-cartesian conversion pattern is in `setup_camera()` — reuse for orbit controls
  - `sys.exit(0)` in keyboard callback cleanly exits GLUT main loop
---

## 2026-04-01 - US-002
- Implemented color-coded 3D coordinate axes: X=red, Y=green, Z=blue
- Added axis labels (X, Y, Z) rendered as bitmap text near positive ends using glutBitmapCharacter
- Axes extend ±5.0 units from origin, labels at 5.3 offset
- Files changed: main.py (added draw_axes and draw_axis_labels functions, called from display)
- **Learnings for future iterations:**
  - `glutBitmapCharacter(GLUT_BITMAP_HELVETICA_18, ord('X'))` renders single characters at glRasterPos3f position
  - glBegin/glEnd blocks with GL_LINES draw line segments between vertex pairs
  - Set glColor3f before glRasterPos3f/glVertex3f to color subsequent primitives
---

## 2026-04-01 - US-003
- Placed 6 wireframe cubes at axis extremities: ±X, ±Y, ±Z at position 4.0 (axis length 5.0)
- Cubes are yellow wireframes (size 0.4) clearly distinguishable from colored axes
- Offset gap between cube centers (4.0) and axis endpoints (5.0) ensures cubes don't touch axis ends
- Files changed: main.py (added draw_cube and draw_cubes functions, called from display)
- **Learnings for future iterations:**
  - glPushMatrix/glPopMatrix isolates transformations per cube — essential for US-004 rotation
  - Wireframe cubes (`glutWireCube`) are more visible against black background than solid cubes without lighting
  - Cube position of 4.0 on axis length 5.0 gives a clear gap; cube half-size (0.2) doesn't reach axis end
---

## 2026-04-01 - US-004
- Implemented continuous rotation animation for all 6 cubes around their respective axes
- Added global `rotation_angle` incremented by 1.0°/frame via `glutTimerFunc` at ~60 FPS (16ms interval)
- Modified `draw_cube` to accept rotation axis parameters; `glRotatef` applied after `glTranslatef` for in-place rotation
- Timer callback re-registers itself for continuous animation, calls `glutPostRedisplay` to trigger redraws
- Files changed: main.py (added animation globals, timer callback, rotation params to draw_cube/draw_cubes)
- **Learnings for future iterations:**
  - Order matters: `glTranslatef` then `glRotatef` rotates the cube in-place at its position; reversing would orbit around origin
  - `glutTimerFunc` is one-shot — must re-register inside the callback for continuous animation
  - `glutPostRedisplay` marks window for redraw without calling display directly — avoids recursion issues
---
